{%- raw -%}
{{- with (include "overlay" . | fromYaml) -}}
{{ if .enable.argocd_application }}
kind: Application
apiVersion: argoproj.io/v1alpha1
metadata:
  name: {{ .argocd_application.name }}
  namespace: {{ .argocd_application.source_namespace }}
spec:
  project: default
  source:
{%- endraw -%}
    repoURL: https://github.com/{{ cookiecutter.github_user }}/{{ cookiecutter.repo }}.git
{%- raw -%}
    targetRevision: master
    path: helm
    helm:
      values: |
        overlay: rancher_desktop
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .argocd_application.target_namespace }}
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
{{ end }}
{{- end -}}
{%- endraw %}